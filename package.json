{
  "name": "@govuk-one-login/data-npm",
  "version": "0.0.6",
  "private": false,
  "description": "Standardised logger for GOV.UK One Login data pod Lambda functions",
  "repository": {
    "type": "git",
    "url": "https://github.com/govuk-one-login/data-npm"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": "./dist/index.js",
  "publishConfig": {
    "@govuk-one-login:registry": "https://npm.pkg.github.com",
    "access": "public",
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --coverage",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint --config oxlint.config.ts --fix",
    "lint:check": "oxlint --config oxlint.config.ts",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "npm run build && changeset publish",
    "secret_scan": "betterleaks dir . -v",
    "secret_scan:git": "betterleaks git .",
    "prepare": "husky"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.25.2",
    "@aws-lambda-powertools/metrics": "^2.25.2",
    "@aws-lambda-powertools/tracer": "^2.25.2"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@tsconfig/node-ts": "^23.6.4",
    "@tsconfig/node24": "^24.0.4",
    "@tsconfig/strictest": "^2.0.8",
    "@types/aws-lambda": "^8.10.126",
    "@vitest/coverage-v8": "^3.0.0",
    "husky": "^9.1.7",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.73.0",
    "oxlint-tsgolint": "^0.24.0",
    "typescript": "^7.0.2",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
